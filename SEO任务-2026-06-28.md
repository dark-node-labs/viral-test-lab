# SEO任务-2026-06-28

## QuickTestHub 每周复盘派发

来源报告：`/Users/wb02/Documents/网站每日检测/每周SEO复盘结果-2026-06-28.md`

本周信号：
- GSC：0 clicks vs 2，736 impressions vs 470，CTR 0%，avg position 35.2 vs 55.2。
- 重点页面：`/sbti-types/` 247 impressions / 0 clicks，`/click-test/` 195 / 0，`/sbti-test/` 146 / 0。
- GA4：Organic Search 3 sessions，key events 0。
- Clarity：首页 13 sessions，有 JavaScript error 模块提示。

## 已执行

- `/sbti-types/`：Title/meta 改为覆盖 “27 SBTI personality types”；增加完整 27 types 总表、每类一句解释、所有类型详情链接，并保留到 `/sbti-test/` 的入口。
- `/click-test/`：补充 5s/10s/30s CPS benchmark table、移动端 tap/CPS 入口、到 `/cps-test/` 的更明确互链；首屏工具仍直接可用。
- 事件：共用 `trackEvent()` 将旧 `tool_start` / `tool_complete` 统一发为 `tool_started` / `tool_completed`；补充 keyboard 与 mouse 核心工具的 started/completed 事件。
- GA4：部署后在生产 `/click-test/` 完成一次 5 秒 CPS 测试，Realtime overview 已看到 `tool_started` 1 次、`tool_completed` 1 次。Admin Events 当前仍只显示旧 `tool_complete` key event，新 `tool_completed` 已实时入站但尚未进入可标记事件列表，需等 GA4 处理延迟后在复查任务中标记。
- Clarity：QuickTestHub 项目 `x0o9x11k1b` 已检查；过去 3 天 17 sessions、首页 13；过去 7 天 31 sessions、首页 21。JavaScript 错误模块过去 3 天和 7 天均为 0 JavaScript 错误总数。
- 继续执行 4 项：优化 `/sbti-test/` Title/meta、首屏和结果解释；首页新增本周搜索入口，加强 `/sbti-test/`、`/sbti-types/`、`/click-test/`、`/cps-test/` 内链；把 `camera-test`、`controller-test`、`mic-test`、`mouse-click-test` 补进 sitemap 并从首页映射短搜索名；核心工具事件补充 `duration_seconds`、`result_value`、计数/结果参数。

## 部署后复查

- 线上验证：首页、`/sbti-types/`、`/click-test/`、`/robots.txt`、`/sitemap.xml` 返回 200。
- 线上页面需能看到 SBTI 27 类型表、click benchmark table 和移动端 CPS 入口。

## 仍需人工处理

- GSC：7 天后复查 `/sbti-types/` 与 `/click-test/` CTR/clicks；必要时对重点 URL 使用 URL Inspection / Request indexing。
- GA4：2026-07-05 复查新 `tool_completed` 是否进入 Admin Events；进入后将 `tool_completed` 标记为 key event，旧 `tool_complete` 可保留用于历史数据。
- Clarity：继续观察 JavaScript error 模块；本次未发现具体报错 URL、堆栈或影响会话。
